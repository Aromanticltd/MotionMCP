{
  "name": "motionmcp",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "bin": {
    "motionmcp": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "mcp": "node src/mcp-server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:publish": "wrangler deploy --env production",
    "worker:tail": "wrangler tail",
    "worker:secret": "wrangler secret put MOTION_API_KEY",
    "docker:build": "docker build -t motion-mcp-server .",
    "docker:run": "docker run -p 8787:8787 --env-file .env motion-mcp-server",
    "docker:compose:up": "docker-compose up -d",
    "docker:compose:down": "docker-compose down",
    "docker:compose:logs": "docker-compose logs -f"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "wrangler": "^3.78.12"
  }
}
